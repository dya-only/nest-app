apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nest-ingress
spec:
  ingressClassName: ingress
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nest-service
            port:
              number: 3000